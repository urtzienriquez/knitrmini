% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{kable}
\alias{kable}
\title{Create a LaTeX table}
\usage{
kable(
  x,
  digits = getOption("digits"),
  row.names = NA,
  col.names = NA,
  align,
  caption = opts_chunk$get("tab.cap"),
  label = NULL,
  format.args = list(),
  escape = TRUE,
  raw = FALSE,
  booktabs = FALSE,
  longtable = FALSE,
  tabular = if (longtable) "longtable" else "tabular",
  valign = if (tabular \%in\% c("tabularx", "xltabular")) "{\\\\linewidth}" else "[t]",
  position = "",
  centering = TRUE,
  vline = getOption("knitr.table.vline", if (booktabs) "" else "|"),
  toprule = getOption("knitr.table.toprule", if (booktabs) "\\\\toprule" else
    "\\\\hline"),
  bottomrule = getOption("knitr.table.bottomrule", if (booktabs) "\\\\bottomrule" else
    "\\\\hline"),
  midrule = getOption("knitr.table.midrule", if (booktabs) "\\\\midrule" else
    "\\\\hline"),
  linesep = if (booktabs) c("", "", "", "", "\\\\addlinespace") else "\\\\hline",
  caption.short = "",
  table.envir = if (!is.null(caption)) "table",
  ...
)
}
\arguments{
\item{x}{A matrix or data frame.}

\item{digits}{Maximum number of digits for numeric columns. Can be a vector
of length \code{ncol(x)}.}

\item{row.names}{Logical: include row names? \code{NA} (default) includes
them if they differ from \code{1:nrow(x)}.}

\item{col.names}{A character vector of column names.}

\item{align}{Column alignment: a character vector of \code{'l'}, \code{'c'},
\code{'r'}, or a single string such as \code{'lcr'}. Numeric columns are
right-aligned and others left-aligned by default.}

\item{caption}{Table caption. Defaults to the chunk option \code{tab.cap}.}

\item{label}{Table label for \code{\\label{}}. Defaults to the current chunk
label. Set to \code{NA} to omit.}

\item{format.args}{A list of arguments passed to \code{\link{format}()}.}

\item{escape}{Logical: escape special LaTeX characters?}

\item{raw}{Logical: when \code{TRUE}, emit only the \code{tabular} content
(no \code{table} environment, no caption, no label, no centering).
Useful when you want to wrap the table in a custom environment or
place the caption/label outside the R chunk.}

\item{booktabs}{Logical: use \pkg{booktabs} rules?}

\item{longtable}{Logical: use \code{longtable} environment?}

\item{tabular}{Name of the tabular environment (e.g. \code{'tabular'},
\code{'longtable'}, \code{'tabularx'}).}

\item{valign}{Vertical alignment of the tabular environment. Use e.g.
\code{'t'} or \code{'b'}. For \code{tabularx} / \code{xltabular} the
default is \code{'{\\linewidth}'}.}

\item{position}{Placement argument for the \code{table} environment
(e.g. \code{'!htb'}).}

\item{centering}{Logical: add \code{\\centering}?}

\item{vline}{Vertical line separator between columns.}

\item{toprule}{Top rule command.}

\item{bottomrule}{Bottom rule command.}

\item{midrule}{Mid rule command (after header).}

\item{linesep}{Line separator between data rows.}

\item{caption.short}{Short caption for the list of tables.}

\item{table.envir}{Name of the floating environment (default \code{'table'}
when caption is not \code{NULL}, otherwise \code{NULL} for no float).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A character vector of the table LaTeX code, with class
\code{'knitr_kable'}.
}
\description{
A minimal table generator for knitrmini. Produces LaTeX tables with
\code{tabular} (or \code{longtable}) environment. Intended for use inside
knitrmini code chunks, but also usable interactively.
}
\examples{
kable(mtcars[1:5, 1:4])
kable(mtcars[1:5, 1:4], caption = "My caption", booktabs = TRUE)
kable(mtcars[1:5, 1:4], raw = TRUE)
}
