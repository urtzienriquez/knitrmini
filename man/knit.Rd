% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knit.R
\name{knit}
\alias{knit}
\title{Knit a document}
\usage{
knit(
  input,
  output = NULL,
  text = NULL,
  quiet = FALSE,
  envir = parent.frame(),
  compile = TRUE,
  engine = NULL,
  minted_style = NULL,
  clean = FALSE
)
}
\arguments{
\item{input}{Path to the input \code{.Rnw} file, or a character vector of text
(when \code{text} is provided).}

\item{output}{Path for the output \code{.tex} file. Auto-guessed from input if \code{NULL}.}

\item{text}{Character vector of document text (used when \code{input} is missing).}

\item{quiet}{Suppress progress messages.}

\item{envir}{Environment in which to evaluate code chunks (default: parent frame).}

\item{compile}{Whether to compile the resulting \code{.tex} to PDF.}

\item{engine}{LaTeX engine (\code{"pdflatex"}, \code{"xelatex"}, \code{"lualatex"}).}

\item{minted_style}{Pygments style name for minted highlighting (e.g. \code{"tango"}).}

\item{clean}{Remove auxiliary files (except \code{.tex} and \code{.pdf}) after compilation.}
}
\value{
Invisibly returns the path to the output \code{.tex} or \code{.pdf} file.
}
\description{
The main entry point for knitrmini. Process an \code{.Rnw} file (or character
vector of text), executing R code chunks and producing a \code{.tex} file.
If \code{compile = TRUE}, the \code{.tex} file is compiled to PDF automatically.
}
\examples{
library(knitrmini)
f <- system.file("examples", "knitrmini-minimal.Rnw", package = "knitrmini")
knit(f, compile = FALSE)

tangle(f)

unlink(c("knitrmini-minimal.tex", "knitrmini-minimal.R"), recursive = TRUE)
}
